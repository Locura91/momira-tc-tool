[pytest]
testpaths = tests
python_files = test_*.py
# Every real Anthropic/Travel Compositor credential is stripped in tests/conftest.py before
# any module-level code runs, so this suite is safe to run anywhere - no network access, no
# API keys, no real database - see conftest.py's module docstring for why.
addopts = -ra
